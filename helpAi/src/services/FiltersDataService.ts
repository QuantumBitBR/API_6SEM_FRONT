


export interface TicketFilters {
  company_id?: string; // IDs separados por vírgula
  product_id?: string; // IDs separados por vírgula
  category_id?: string; // IDs separados por vírgula
  priority_id?: string; // IDs separados por vírgula
  createdat?: string; // Data início (YYYY-MM-DD)
  end_date?: string; // Data fim (YYYY-MM-DD)
}
